module.exports.run = async (client, message, args, config) => {
  return;/*
  client
    .sendText(
      message.from,
      `1- *Como posso fazer stickers?*
R= Digite #sticker na foto e eu farei um sticker.

2- *Gostaria de dar sugestões/reportar erros ao bot.*
R= Aqui o número do desenvolvedor: wa.me/${config.numero_dev}
`
    )
    .then((result) => {
      console.log("Resultado: ", result);
    })
    .catch((erro) => {
      console.error("Erro quando estou enviando: ", erro);
    }); */
};
